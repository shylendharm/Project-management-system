// src/tests/setup.js
// This file runs before every test file in Vitest.
// It extends the expect matchers with @testing-library/jest-dom matchers
// e.g. toBeInTheDocument(), toHaveTextContent(), etc.
import '@testing-library/jest-dom';
